/**
 * SEO metadata for the Search / Saved Routes page.
 * Provides a unique title and description for search engine indexing.
 */
export const metadata = {
    title: 'Rute Tersimpan — Pangandaran.ai',
    description:
        'Lihat dan kelola rute wisata yang telah Anda simpan. Temukan kembali destinasi favorit Anda di Pangandaran.',
};

export default function SearchLayout({ children }) {
    return children;
}
